{
    "buildCommand": "npm install && npm run build",
    "outputDirectory": "dist/clicker/browser",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}